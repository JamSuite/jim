---
title: "{group} — blueprint"
group: "{group}"
kind: blueprint
updated: "{YYYY-MM-DD}"
last_full_generate: "{YYYY-MM-DDThh:mm:ssZ}"
---

# {group} — blueprint

*The current, present-tense specification of the `{group}` group: its
responsibilities, exposed and required surface, structure, and load-bearing
invariants. Generated by `/jim:blueprint {group}`; every claim traces to the
group's specs, ARCHITECTURE.md, and code.*

## Responsibility

{What this group is for — one or two paragraphs, grounded in the group's specs.}

## Provides

{The surface this group exposes for other groups to depend on. One entry per
exposed operation / type / contract, with the guarantee attached. If the group
exposes nothing across its boundary, write "*None — internal to the group.*"}

- `{surface}` — {guarantee}

## Requires

{What this group depends on from other groups, discovered from its code.
Best-effort at this scope: record only cross-group dependencies grounded in the
code; where the boundary is unclear, say so rather than inventing. If the group
depends on nothing outside itself, write "*None.*"}

- `{other-group}.{surface}` — {guarantee relied on}

## Structure

{Components and key abstractions — grounded in the group's plan(s),
ARCHITECTURE.md, and code. Name real files and modules.}

## Invariants

{The load-bearing constraints the group's code must uphold — behavioral,
structural, and code-shape. Capture the *rule*, never per-instance
implementation. Each carries a criticality and the intended way it would be
verified.}

| Invariant | Criticality | Verification method |
| :--- | :--- | :--- |
| {constraint the code must satisfy} | critical / high / medium / low | {e.g. lint / type / AST check, test, or judge} |
